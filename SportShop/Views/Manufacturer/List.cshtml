@model IEnumerable<Manufacturer>
@foreach (var item in Model)
{
    <div>
        <h3>@item.Name</h3>
        <h5>@item.Country</h5>
        @foreach (var product in @item.Products)
        {
            <h6>@product.Name</h6>
        }
    </div>
}